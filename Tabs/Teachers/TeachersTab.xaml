<UserControl x:Class="classique.timetabler.Tabs.Teachers.TeachersTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:classique.timetabler.Tabs.Teachers"
             xmlns:controls="clr-namespace:classique.timetabler.Controls"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="DayOfWeekValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="sys:DayOfWeek"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Teachers List -->
        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Teachers" 
                       FontSize="24" 
                       FontWeight="Light" 
                       Margin="0,0,0,10"/>

            <ListBox Grid.Row="1" 
                     x:Name="TeachersListBox"
                     ItemsSource="{Binding Teachers}"
                     SelectionChanged="TeachersListBox_SelectionChanged"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     Margin="0,0,0,10">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="2">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#9888; " Foreground="#B8860B" 
                                           Visibility="{Binding HasWarnings, Converter={StaticResource BoolToVis}}"
                                           ToolTip="{Binding WarningsSummary}"/>
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                            </StackPanel>
                            <TextBlock Text="{Binding WorkloadSummary}" Foreground="Gray" FontSize="11"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <Button Content="Add" 
                        Width="60" 
                        Height="30" 
                        Margin="0,0,10,0"
                        Click="AddTeacher_Click"/>
                <Button Content="Remove" 
                        Width="60" 
                        Height="30"
                        Click="RemoveTeacher_Click"/>
            </StackPanel>
        </Grid>

        <!-- Teacher Details / Availability -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Availability" 
                       FontSize="24" 
                       FontWeight="Light" 
                       Margin="0,0,0,10"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Teacher Name:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="TeacherNameTextBox" 
                         Width="200" 
                         TextChanged="TeacherNameTextBox_TextChanged"/>
            </StackPanel>

            <!-- Workload Summary Panel -->
            <Border Grid.Row="2" 
                    Background="#E8F4FD" 
                    BorderBrush="#B8D4E8" 
                    BorderThickness="1" 
                    Padding="10" 
                    Margin="0,0,0,10"
                    x:Name="WorkloadPanel"
                    Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Groups" FontWeight="SemiBold" Foreground="#2C5282"/>
                        <TextBlock x:Name="GroupsCountText" Foreground="#2C5282"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Solos" FontWeight="SemiBold" Foreground="#2C5282"/>
                        <TextBlock x:Name="SolosCountText" Foreground="#2C5282"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Total / Available" FontWeight="SemiBold" Foreground="#2C5282"/>
                        <TextBlock x:Name="TotalWorkloadText" Foreground="#2C5282"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Warning Panel -->
            <Border x:Name="WarningPanel" 
                    Grid.Row="3" 
                    Background="#FFF3CD" 
                    BorderBrush="#FFE69C" 
                    BorderThickness="1" 
                    Padding="10" 
                    Margin="0,0,0,10"
                    Visibility="Collapsed">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#9888; " Foreground="#856404" FontWeight="Bold"/>
                    <TextBlock x:Name="WarningText" Foreground="#856404" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <DataGrid Grid.Row="4" 
                      x:Name="AvailabilityDataGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single"
                      HorizontalAlignment="Stretch"
                      CellEditEnding="AvailabilityDataGrid_CellEditEnding"
                      Margin="0,0,0,10">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Studio" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding StudioName}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding DataContext.Studios, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                          SelectedValue="{Binding StudioId, UpdateSourceTrigger=PropertyChanged}"
                                          SelectedValuePath="Id"
                                          DisplayMemberPath="Name"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                    <DataGridComboBoxColumn Header="Day"
                                            SelectedItemBinding="{Binding Day, UpdateSourceTrigger=PropertyChanged}"
                                            ItemsSource="{Binding Source={StaticResource DayOfWeekValues}}"
                                            Width="*"/>
                    <DataGridTemplateColumn Header="Start" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding StartTime, StringFormat='{}{0:h:mm tt}'}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <controls:TimeOnlyPicker Time="{Binding StartTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="End" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding EndTime, StringFormat='{}{0:h:mm tt}'}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <controls:TimeOnlyPicker Time="{Binding EndTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="5" Orientation="Horizontal">
                <Button Content="Add Slot" 
                        Width="80" 
                        Height="30" 
                        Margin="0,0,10,0"
                        Click="AddAvailability_Click"/>
                <Button Content="Remove Slot" 
                        Width="80" 
                        Height="30"
                        Click="RemoveAvailability_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>